nelmio_api_doc:
    documentation:
        info:
            title: '%app.name%'
            version: 1.0.0
        servers:
            - url: '%app.url%'
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
    areas:
        path_patterns:
            - ^/auth
            - ^/users
            - ^/fixtures